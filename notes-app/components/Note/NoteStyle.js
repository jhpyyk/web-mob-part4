import { StyleSheet, View } from "react-native";

const NoteStyle = StyleSheet.create({
  container: {
    marginBottom: 2,
    padding: 3,
    borderWidth: 1,
    borderRadius: 4,
    borderColor: "#FFF",
  },
});

export default NoteStyle;
